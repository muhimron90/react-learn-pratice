{
  "name": "react-graphql-basic",
  "version": "1.0.0",
  "description": "react express grapql mongodb stacks",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"yarn client\" \"yarn server\" ",
    "client": "cd web-spa && yarn start",
    "server": "cd server && yarn dev",
    "dir-client": "cd web-spa && yarn ",
    "dir-server": "cd server && yarn ",
    "install:modules": "concurrently \"yarn\"  \"yarn dir-client\" \"yarn dir-server \" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Muhamad Imron",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
